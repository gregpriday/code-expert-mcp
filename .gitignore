# Secrets
.env
.env.*
!.env.example
*.pem
*.key

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp

# Go binaries & plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Compiled output for this module
/codeexpert
/CodeExpertMCP
/bin/
/dist/

# Go test & coverage / profiling output
*.test
*.out
coverage.*
*.prof
*.pprof

# Go workspace files (local, not shared)
go.work
go.work.sum

# Dependency directory (uncomment if you do not commit vendored deps)
# vendor/

# Local runtime cache/run artifacts, only if cache.dir is pointed inside the repo
# (the cache defaults to the OS user cache dir and stays out of the repo)
.codeexpert/

# Local scratch/temp directory
.tmp/
